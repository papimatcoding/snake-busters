{
  "name": "snake-busters",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "node --check dist/engine.js && node --check dist/game.js && node --check server.mjs"
  },
  "engines": { "node": ">=20" }
}
